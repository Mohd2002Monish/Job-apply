{
  "name": "job-apply-app",
  "version": "1.0.0",
  "private": true,
  "description": "Root runner for Job Apply App",
  "scripts": {
    "dev": "npx concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run start --prefix Back-end\" \"npm run dev --prefix Front-end\"",
    "start": "npm run dev",
    "backend": "npm run start --prefix Back-end",
    "frontend": "npm run dev --prefix Front-end"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
